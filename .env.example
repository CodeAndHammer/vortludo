# Vortludo Environment Configuration Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# APPLICATION MODE
# =============================================================================

# Set application mode - affects logging, caching, and security headers
# Options: "release" (production) or "debug" (development)
# GIN_MODE=release

# Alternative way to set production mode (used with GIN_MODE)
# ENV=production

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Port for the HTTP server to listen on
# PORT=8080

# =============================================================================
# SESSION & COOKIE CONFIGURATION
# =============================================================================

# Maximum age for session and CSRF cookies
# Should be longer than your service spin-down time for serverless
# Examples: 45m (serverless), 1h (production), 2h (development)
# COOKIE_MAX_AGE=45m

# Session timeout - how long inactive sessions are kept in memory
# Should be longer than service spin-down time for serverless
# Examples: 30m (serverless), 1h (production), 2h (development)
# SESSION_TIMEOUT=30m

# =============================================================================
# CACHING
# =============================================================================

# Cache duration for static assets (CSS, JS, images)
# Shorter for development, longer for production
# Examples: 0s (no cache), 5m (development), 1h (production)
# STATIC_CACHE_AGE=5m

# =============================================================================
# RATE LIMITING
# =============================================================================

# Rate limit: requests per second per IP address
# Lower for production, higher for development
# Examples: 5 (production), 100 (development)
# RATE_LIMIT_RPS=5

# Rate limit burst: maximum burst size for rate limiting
# Should be higher than RPS for smooth user experience
# Examples: 10 (production), 200 (development)
# RATE_LIMIT_BURST=10

# =============================================================================
# PRESET CONFIGURATIONS
# =============================================================================

# -----------------------------------------------------------------------------
# SERVERLESS CONFIGURATION (15-minute spin-down)
# -----------------------------------------------------------------------------
# GIN_MODE=release
# PORT=8080
# COOKIE_MAX_AGE=45m
# SESSION_TIMEOUT=30m
# STATIC_CACHE_AGE=5m
# RATE_LIMIT_RPS=5
# RATE_LIMIT_BURST=10

# -----------------------------------------------------------------------------
# PRODUCTION CONFIGURATION
# -----------------------------------------------------------------------------
# GIN_MODE=release
# PORT=8080
# COOKIE_MAX_AGE=1h
# SESSION_TIMEOUT=1h
# STATIC_CACHE_AGE=1h
# RATE_LIMIT_RPS=10
# RATE_LIMIT_BURST=20

# -----------------------------------------------------------------------------
# DEVELOPMENT CONFIGURATION
# -----------------------------------------------------------------------------
# GIN_MODE=debug
# PORT=3000
# COOKIE_MAX_AGE=2h
# SESSION_TIMEOUT=2h
# STATIC_CACHE_AGE=0s
# RATE_LIMIT_RPS=100
# RATE_LIMIT_BURST=200